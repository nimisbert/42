-- Modules to Test
-- 
import Test.Hspec

main :: IO ()
main = putStrLn "Test suite not yet implemented"
--main = hspec $ do
--    describe "Straight Line" $ do
--        it "(-3, 10) (1, 4)" $ lineFromPoints (-3, 10) (1, 4) `shouldBe` (-3/2, (-3, 10))

--    describe "Mid Point of a Segment" $ do
--        it "(1, -3) (-2, 1)" $ segmentMidPoint (1, -3) (-2, 1) `shouldBe` (-1/2, -1)
    
--    describe "Length of a Segment" $ do
--        it "(3, 13) (4, 18)" $ segmentLength (3, 13) (4, 18) `shouldBe` sqrt 26